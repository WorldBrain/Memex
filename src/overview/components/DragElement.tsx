import React, { PureComponent } from 'react'

class DragElement extends PureComponent {
    render() {
        return <div id="dragged-element"> + Drop into Collection</div>
    }
}

export default DragElement
