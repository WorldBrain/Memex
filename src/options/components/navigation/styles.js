const nav = {
    
};

const navItem = {

};

const navLink = (isActive) => ({
    color: isActive ? 'red' : 'black'
});

export default {
    root: {
        
    },
    nav,
    navItem,
    navLink
};
